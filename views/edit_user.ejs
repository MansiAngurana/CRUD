<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User</title>
    <link rel="stylesheet" href="/public/style.css"> <!-- Your custom styles -->
</head>
<body>
    <header>
        <h1>Edit User</h1>
    </header>

    <div class="container">
        <h2>Edit User Details</h2>

        <form action="/update/<%= User._id %>" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label>Name</label>
                <input type="text" name="name" value="<%= User.name %>" placeholder="Enter your Name" class="form-control custom-input" required>
            </div>
            <div class="form-group">
                <label>Email</label>
                <input type="email" name="email" value="<%= User.email %>" class="form-control custom-input" required>
            </div>
            <div class="form-group">
                <label>City</label>
                <input type="text" name="city" value="<%= User.city %>" class="form-control custom-input" required>
            </div>
            <div class="form-group">
                <label>Phone</label>
                <input type="text" name="phone" value="<%= User.phone %>" class="form-control custom-input" required>
            </div>

            <!-- Current Profile Image -->
            <div class="form-group">
                <label>Current Profile Image</label><br>
                <% if (User.img) { %>
                    <img src="/uploads/<%= User.image %>" width="100">
                <% } else { %>
                    No Image
                <% } %>
            </div>

            <!-- New Profile Image -->
            <div class="form-group">
                <label>New Profile Image (Optional)</label>
                <input type="file" name="image" class="form-control">
                <!-- Pass old image filename as hidden input for reference -->
                <input type="hidden" name="old_name" value="<%= User.img %>">
            </div>

            <button type="submit" class="btn btn-primary">Update User</button>
        </form>
    </div>
    <%- include('layout/footer') %>
</body>
</html>


